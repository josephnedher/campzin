A simple Django Proect 
  
  # how to try this ?
      just clone the project and extract
      # cd redfab
      # python3 manage.py migrate
      # python3 manage.py makemigrations
      # python3 manage.py migrate
